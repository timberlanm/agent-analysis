Flask
flask-cors
PyYAML

# Resolve the mutually compatible SQLAlchemy/greenlet versions available for
# the target Linux/Python environment.  greenlet is listed explicitly so pip
# can upgrade an incompatible preinstalled copy instead of retaining it.
SQLAlchemy
greenlet
psycopg[binary]
alembic
