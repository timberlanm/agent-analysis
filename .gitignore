# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/

# Node / Frontend
node_modules/
dist/
.cache/
.parcel-cache/
.npm-cache/
# 本项目使用 npm（package-lock.json）；忽略 pnpm 产物避免混淆
pnpm-lock.yaml
pnpm-workspace.yaml

# IDE
.vscode/
.idea/
*.swp

# OS
Thumbs.db
.DS_Store

# Runtime data —— 演示用伪造数据随仓库提交，便于生产环境克隆后直接展示告警与截图
# （如需改回“不跟踪运行数据”，取消下面两行注释即可）
# backend/uploads/
# backend/data/
# 但每部署独有的会话签名密钥绝不入库（缺失时应用会自动重新生成）
backend/data/secret_key

# CodeBuddy
.codebuddy/

# Logs
*.log
flask.log
flask_err.log
